<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/Theme.CardView.Outer">

    <LinearLayout style="@style/Theme.CardView.InnerLayout">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:layout_gravity="center">

            <TextView
                android:id="@+id/category_card_title"
                style="@style/Theme.CardTitle" />

            <TextView
                android:id="@+id/category_card_rule_name"
                style="@style/Theme.CardDesc" />
        </LinearLayout>

        <Button
            android:id="@+id/category_card_edit_button"
            style="@style/Theme.CardButtonEdit" />

        <Button
            android:id="@+id/category_card_delete_button"
            style="@style/Theme.CardButtonRemove" />
    </LinearLayout>
</androidx.cardview.widget.CardView>